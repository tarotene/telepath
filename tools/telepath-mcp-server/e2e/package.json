{
  "name": "telepath-mcp-inspector-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --project inspector-ui",
    "codegen": "playwright codegen http://localhost:6274"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
